
import React from 'react';
import Dashboard from './Dashboard';

const Index = () => {
  // Render the Dashboard component directly instead of redirecting
  return <Dashboard />;
};

export default Index;
